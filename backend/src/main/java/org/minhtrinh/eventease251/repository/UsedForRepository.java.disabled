package org.minhtrinh.eventease251.repository;

import org.minhtrinh.eventease251.entity.UsedFor;
import org.minhtrinh.eventease251.entity.UsedForId;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface UsedForRepository extends JpaRepository<UsedFor, UsedForId> {
}